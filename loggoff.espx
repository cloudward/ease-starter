<# if ("<#[url.goto]#>" != "") {
	set cookie.keypassn to "";
	set cookie.memberkey to "";
	set cookie.first to "";
	set cookie.last to "";
	set cookie.email to "";
	set cookie.company to "";
	set cookie.cid to "";
	redirect to "/?page=<#[url.goto]#>";
} else {
	set cookie.keypassn to "";
	set cookie.memberkey to "";
	set cookie.first to "";
	set cookie.last to "";
	set cookie.email to "";
	set cookie.company to "";
	set cookie.cid to "";
	redirect to "/?page=logged_off";
}
#>
